import { EmployeesDataFilteringPipe } from './employees-data-filtering.pipe';

describe('EmployeesDataFilteringPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeesDataFilteringPipe();
    expect(pipe).toBeTruthy();
  });
});
